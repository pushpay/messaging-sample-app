{
  "name": "messaging-campaign",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd client; npm run build; cd ..; node server.js",
    "install:all": "npm install; cd client; npm install; cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "dotenv": "^6.2.0",
    "express": "^4.16.4",
    "mariadb": "^2.0.3",
    "node-bandwidth": "^3.0.0",
    "socket.io": "^2.2.0"
  }
}
